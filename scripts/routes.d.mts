// Type declaration for routes.mjs so consumers (CLI unit tests) typecheck
// without enabling allowJs. The implementation stays a dependency-free .mjs the
// template build can run directly with node.
export function enumerateStaticRoutes(routerSource: string): string[];
